# no-mistakes pipeline configuration for AI Travel Planner (Next.js + TypeScript)

agent: pi

commands:
  test: "npm test 2>&1 | tail -30"
  lint: "npm run lint 2>&1 | tail -30"
  format: "npm run format 2>&1 | tail -30"
